#!/usr/bin/env ruby
$:.unshift File.join(File.dirname(__FILE__), '..', 'lib')

require 'rubygems'
require 'configliere'
require 'genomer'

Settings.use :commandline
Settings.resolve!

begin
  puts Genomer::Runtime.new(Settings).execute!
  exit 0
rescue GenomerError => e
  STDERR.puts "Error. #{e.message}"
  exit 1
end
